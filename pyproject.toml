[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ovos_solver_failure_plugin"
dynamic = ["version"]
description = "default error message for OpenVoiceOS personas"
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "JarbasAi", email = "jarbasai@mailfence.com"}]
keywords = ["OpenVoiceOS", "OVOS", "plugin", "utterance", "fallback", "query"]
dependencies = [
    "ovos-plugin-manager>=2.3.0a1,<3.0.0",
]

[project.optional-dependencies]
test = [
    "pytest",
]

[project.urls]
Homepage = "https://github.com/OpenVoiceOS/ovos-solver-failure-plugin"

[project.entry-points."opm.agents.retrieval"]
"ovos-solver-failure-plugin" = "ovos_solver_failure_plugin:DefaultFailureMessage"

[tool.setuptools.dynamic]
version = {attr = "ovos_solver_failure_plugin.version.__version__"}

[tool.setuptools.packages.find]
where = ["."]
include = ["ovos_solver_failure_plugin*"]

[tool.setuptools.package-data]
ovos_solver_failure_plugin = ["*.json", "locale/**/*"]
